<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Blog Post</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.css"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="min-h-screen bg-gray-50 dark:bg-gray-900">
      <!-- Welcome Message -->
      <div class="flex justify-between items-center w-full p-8 mb-8 bg-white rounded-xl shadow-lg dark:bg-gray-800">
        <div>
          <h1 class="text-3xl font-bold text-gray-800 dark:text-gray-100">Edit your Post!, <%= user.name %>!</h1>
          <p class="mt-2 text-gray-600 dark:text-gray-400">Edit the blog post.....</p>
        </div>
        <a href="/logout" class="px-4 py-2 font-semibold text-white bg-red-500 rounded-md hover:bg-red-600 dark:bg-red-700 dark:hover:bg-red-800">
          Logout
        </a>
      </div>

      <!-- Blog Post Form -->
      <div class="flex flex-col items-center justify-center w-full max-w-2xl mx-auto p-8 space-y-4 bg-white rounded-xl shadow-lg dark:bg-gray-800">
        <form action="/post/edited/<%= post.id %>" method="POST" class="w-full">
          <div class="mb-4">
            <label for="title" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Title</label>
            <input type="text" id="title" name="title" required class="block w-full mt-1 p-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-gray-300" value="<%= post.title %>"/>
          </div>

          <div class="mb-4">
            <label for="content" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Content</label>
            <textarea id="content" name="content" rows="5" required class="block w-full mt-1 p-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-gray-300 resize-none" placeholder="What's on your mind?"><%= post.content %></textarea>
          </div>

          <div class="text-center">
            <button type="submit" class="px-4 py-2 font-semibold text-white bg-yellow-500 rounded-md hover:bg-yellow-600 dark:bg-blue-700 dark:hover:bg-blue-800">
              Edit Post!
            </button>
          </div>
        </form>
      </div>
        </div>
      </div>
    </div>
  </body>
</html>
